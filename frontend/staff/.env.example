# Copy to .env.local. Not needed for local dev (Vite proxies /api -> the
# backend, see vite.config.ts). For production builds, use
# .env.production.example instead (copy to .env.production.local) so the
# deployed staff app points at your Render backend.
# VITE_API_BASE_URL=https://swasthyasetu-api.onrender.com
