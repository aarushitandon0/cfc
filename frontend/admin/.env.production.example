# Copy to .env.production.local (gitignored via *.local) before running
# `npm run build` for deployment. Not needed for local dev - see .env.example.
VITE_API_BASE_URL=https://swasthyasetu-api.onrender.com
